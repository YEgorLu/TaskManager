import { FC } from "react";

const Header: FC = () => {
  return (
    <div className="flex h-[50px] w-screen items-center justify-center bg-blue-500 text-center text-white">
      <span className="m-0 leading-4">header</span>
    </div>
  );
};

export default Header;
